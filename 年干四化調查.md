# 年干四化調查

## 結論

`SiHua_Table` 的十組對照和 iztro 目前預設四化表完全一致，且 `applySiHua()` 以「祿、權、科、忌」的固定順序套用，12 宮星曜的本命四化規則正確。

中宮資訊面板則以瀏覽器當日換算農曆年，顯示該年的流年四化與農曆年、干支。它以 `Stems.indexOf()` 取得天干索引，不使用 Unicode 碼位相減。

本調查確認的是此專案採用的 iztro 預設實作，不宣稱它是所有流派或歷史文本的唯一規則。

## 對照表

欄位順序均為祿、權、科、忌。iztro 的 [原始碼資料表](https://github.com/SylarLong/iztro/blob/2c7ef9be669df7b19d1799f4dce335fed3794f78/src/data/heavenlyStems.ts#L14-L84) 與本專案 `SiHua_Table` 的每一列相同。

| 年干 | 祿 | 權 | 科 | 忌 | 對照結果 |
| --- | --- | --- | --- | --- | --- |
| 甲 | 廉貞 | 破軍 | 武曲 | 太陽 | 一致 |
| 乙 | 天機 | 天梁 | 紫微 | 太陰 | 一致 |
| 丙 | 天同 | 天機 | 文昌 | 廉貞 | 一致 |
| 丁 | 太陰 | 天同 | 天機 | 巨門 | 一致 |
| 戊 | 貪狼 | 太陰 | 右弼 | 天機 | 一致 |
| 己 | 武曲 | 貪狼 | 天梁 | 文曲 | 一致 |
| 庚 | 太陽 | 武曲 | 太陰 | 天同 | 一致 |
| 辛 | 巨門 | 太陽 | 文曲 | 文昌 | 一致 |
| 壬 | 天梁 | 紫微 | 左輔 | 武曲 | 一致 |
| 癸 | 破軍 | 巨門 | 太陰 | 貪狼 | 一致 |

## 實作檢查

### 命盤標記

本專案在 `紫微斗數.html:528` 以農曆年 `lYear` 算出 0 到 9 的年干索引，並在 `:541` 傳入 `applySiHua()`。`applySiHua()` 於 `:591-592` 依序將資料列的四顆星標為祿、權、科、忌。因此，對照表的資料和套用順序都正確。

`lYear` 來自輸入生日轉換後的 `finalLunar.getYear()`，用於 12 宮星曜的本命四化。中宮面板另以 `Solar.fromDate(new Date()).getLunar()` 取得瀏覽器當日的農曆年，使用該年的天干查同一張表，並標示為「流年四化」。

### 資訊面板

`紫微斗數.html` 的中宮面板以 `ZWDSEngine.Stems.indexOf(currentGanzhi[0])` 取得天干索引。十天干的 Unicode 碼位並非連號，故不可用字碼相減推算索引。

### 年界範圍

本專案的本命四化與中宮流年四化都以農曆正月初一為年界，而非立春。23:00 之後的出生資料會先移至次日，再決定本命農曆年。

iztro 的官方文件也把出生年干支年界列為可選設定，`normal` 是農曆新年，`exact` 是立春，預設為 `normal`：[Configuration and Plugins](https://docs.iztro.com/en_US/posts/config-n-plugin)。本專案沒有提供立春切換選項，因此接近立春、但尚未跨農曆新年的出生日期，可能與採立春年界的排盤結果不同。這是年界取法的範圍差異，不是上述四化對照表的錯誤。

## 來源與信賴範圍

- [iztro `heavenlyStems.ts` 固定版本原始碼](https://github.com/SylarLong/iztro/blob/2c7ef9be669df7b19d1799f4dce335fed3794f78/src/data/heavenlyStems.ts#L14-L84)：本調查的主要規則參考，可直接檢查十組資料與欄位順序。
- [iztro `getTargetMutagens()` 固定版本原始碼](https://github.com/SylarLong/iztro/blob/2c7ef9be669df7b19d1799f4dce335fed3794f78/src/utils/index.ts#L20-L30)：確認預設使用上述資料表，除非呼叫端另行設定覆寫表。
- [iztro 年界設定固定版本原始碼](https://github.com/SylarLong/iztro/blob/2c7ef9be669df7b19d1799f4dce335fed3794f78/src/astro/astro.ts#L36-L45)：確認 `normal` 為農曆正月初一，`exact` 為立春。
- [iztro 官方設定文件](https://docs.iztro.com/en_US/posts/config-n-plugin)：年干支年界與可自訂四化表的使用文件。

兩者是受維護程式庫的程式碼及文件，能證實與 iztro 預設行為是否一致，但不是古籍影像、校勘本或歷史學術研究，不能單獨作為歷史傳承的文本權威。

## 驗證指令

在專案根目錄執行：

```bash
rg -n 'SiHua_Table|applySiHua|yearGan|currentGanzhi|currentYear' 紫微斗數.html
```
